<% result = @favorites.empty? ? 'noresult': @favorites %>
$('#all-favorites').append('<%= j render result %>');

<% if @favorites.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@favorites) %>');
<% else %>
  $('.pagination').hide();
<% end %>